---
import Header from '../components/Header.astro';
import Head from '../components/Head.astro';
---


<html lang="en">
  <Head title="Contact" />
  <body class="bg-gray-50 text-gray-800">
    <Header />

    <main class="py-16">
      <section class="max-w-5xl mx-auto px-4">
        <div class="text-center mb-12">
          <h1 class="text-4xl font-semibold text-gray-900">Contact</h1>
          <p class="text-lg text-gray-600 mt-1">なにかメッセージがあればご連絡ください。お手数ですが、３日経っても返信がない場合はもう一度お送りください！</p>
        </div>

        <form action="https://formspree.io/f/xbldyyyw" method="POST" class="bg-white p-6 rounded-lg shadow-xl border border-gray-200 max-w-md mx-auto">

          <div class="mb-6">
            <label for="name" class="block text-lg font-semibold text-gray-800">名前 / Name</label>
            <input type="text" id="name" name="name" required class="w-full p-4 mt-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
          </div>

          <div class="mb-6">
            <label for="message" class="block text-lg font-semibold text-gray-800">メッセージ / Message</label>
            <textarea id="message" name="message" required class="w-full p-4 mt-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" rows="2"></textarea>
          </div>

          <div class="text-center">
            <button type="submit" class="w-full py-3 bg-blue-500 text-white font-semibold rounded-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-400">
              送信 / Submit
            </button>
          </div>
        </form>
      </section>
    </main>

    <script type="module" src="/script/global.js"></script>
  </body>
</html>